server:
  port: 9040

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/razorpay-vault-db

vault:
  master-key: g+/QQX89fndYZmgNNJWs7u3YZMmCaoo2wef+6pqyK3k=

resilience4j:
  thread-pool-bulkhead:
    instances:
      vault-card-processor:
        max-thread-pool-size: 10
        core-thread-pool-size: 5
        queue-capacity: 20  

---

spring:
  config:
    activate:
      on-profile: k8s
  datasource:
    url: jdbc:postgresql://postgres.razorpay-core.svc.cluster.local:5432/razorpay-vault-db
    username: vault-user
    password: ${VAULT_DB_PASSWORD}

vault:
  master-key: ${VAULT_MASTER_KEY}