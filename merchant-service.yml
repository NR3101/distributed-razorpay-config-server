server:
  port: 9010

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/razorpay-merchant-db

jwt:
  secret-key: only-for-dev-env-fduagsdiudiuadiuasiduavsduvu67etdhguhd9w8rdyw98fhoishncfijfdc90njsc

webhook:
  secret-encryption-key: 4wH50N/hWWWT4MUPex7vdYg3LVG55Y/j0BiM1eqSDCM=

---

spring:
  config:
    activate:
      on-profile: k8s
  datasource:
    url: jdbc:postgresql://postgres.razorpay-core.svc.cluster.local:5432/razorpay-merchant-db
    username: merchant_user
    password: ${MERCHANT_DB_PASSWORD}

jwt:
  secret-key: ${JWT_SECRET}

webhook:
  secret-encryption-key: ${WEBHOOK_SECRET_ENCRYPTION_KEY}
